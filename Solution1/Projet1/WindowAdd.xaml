<Window x:Class="Projet1.WindowAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet1"
        mc:Ignorable="d"
        Title="WindowAdd" Height="400" Width="600" 
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">
    
    <!-- Bordure de la fenêtre -->
    <Border Background="{StaticResource backgroundColor}" BorderBrush="{StaticResource titleColor}" BorderThickness="1.5" CornerRadius="15">
        <DockPanel>
            <!-- Texte Ajouter Titre-->
            <TextBlock Text="Ajout Titre" Margin="5" DockPanel.Dock="Top" HorizontalAlignment="Center" FontSize="48" Foreground="{StaticResource titleColor}"/>
            <!-- UserControl3 (Ajouter)-->
            <local:UserControl3 x:Name="ucAdd"/>
        </DockPanel>
    </Border>
</Window>
